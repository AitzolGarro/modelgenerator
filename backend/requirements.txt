# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 3D processing
trimesh>=4.0.0
numpy>=1.26.0
Pillow>=10.0.0

# ML - Install PyTorch with CUDA first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.2.0
torchvision>=0.17.0

# ML - Text to Image
diffusers>=0.28.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
peft>=0.10.0
bitsandbytes>=0.43.0
ftfy>=6.0.0

# Background removal (optional)
rembg>=2.0.0

# UV Texturing Pipeline
# pyrender requires EGL for headless rendering (set PYOPENGL_PLATFORM=egl)
# xatlas requires Python dev headers (python3-devel / python3.11-dev) + C++ compiler
# On Bazzite/host: scipy LSCM fallback is used instead of xatlas
# In Docker: xatlas, pyrender, opencv-python-headless are installed separately (see Dockerfile)
scipy>=1.12.0
bvh>=0.3
scikit-image>=0.22.0
# Optional — install manually in Docker (need headers to build):
# xatlas>=0.0.11
# pyrender
# PyOpenGL==3.1.0
# opencv-python-headless
